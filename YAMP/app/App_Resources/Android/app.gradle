// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.debugstudios.yamp"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
