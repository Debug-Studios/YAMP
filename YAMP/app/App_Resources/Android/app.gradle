// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.debugstudios.emno"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
